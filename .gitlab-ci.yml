variables:
  DOCKER_HOST: tcp://docker:2375/
  QUANTUM_CLI: registry.internal.planetary-networks.de/quantum-public/cli:2
  DOCKER_AUTH_CONFIG: '{ "auths": { "https://index.docker.io/v1/": { "auth": "$DOCKER_AUTH" } }}'

stages:
  - build
  - build_release
  - deploy_release

services:
  - docker:19.03.3-dind

before_script:
  - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY
  - docker login -u $DOCKERHUB_LOGIN -p $DOCKERHUB_PASSWORD


docker-build:
  variables:
    IMAGE: $CI_REGISTRY_IMAGE
    DOCKERFILE: Dockerfile
  stage: build
  allow_failure: false
  script:
    - docker pull $IMAGE:latest || true
    - docker build --cache-from $IMAGE:latest --tag $IMAGE:$CI_COMMIT_SHA --tag $IMAGE:latest -f $DOCKERFILE .
    - docker push $IMAGE:$CI_COMMIT_SHA
    - docker push $IMAGE:latest
  only:
    - releases

.docker_build_release:
  stage: build_release
  image:
    name: docker/compose:1.25.0
    entrypoint: ["/bin/sh", "-c"]
  cache:
    key: "${CI_PIPELINE_ID}-${PROJECT}"
    paths: [quantum.yml]
  script:
    - docker-compose -f docker-compose.yml -f stack.yml config > quantum.yml
  tags: [v2]
  allow_failure: false
  when: on_success
  only:
    - releases

.deploy_release:
  stage: deploy_release
  needs: ["docker-build"]
  image: $QUANTUM_CLI
  cache:
    key: "${CI_PIPELINE_ID}-${PROJECT}"
    paths: [quantum.yml]
  script:
    - quantum-cli stack update --create --stack $PROJECT$SERVICENAME-$QUANTUM_ENDPOINT --wait
  tags: [v2]
  allow_failure: false
  when: on_success
  only:
    - releases

# List of Releases:
#
# release/bb/frankfurt
# release/bb/kyritz
# release/bb/birkenwerder
# release/bb/bad-belzig
# release/bb/gransee
# release/bb/falkenberg-elster
# release/bb/herzberg-elster
# release/bb/michendorf
# release/bb/amt-schlieben
# release/bb/eisenhuettenstadt
# release/bb/angermuende
# release/bb/seelow

#
# List of releases to build
#
bb-frankfurt-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-frankfurt
    DOMAINNAME: bb-frankfurt
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-kyritz-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-kyritz
    DOMAINNAME: bb-kyritz
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-birkenwerder-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-birkenwerder
    DOMAINNAME: bb-birkenwerder
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-bad-belzig-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-bad-belzig
    DOMAINNAME: bad-belzig
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-gransee-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-gransee
    DOMAINNAME: bb-gransee
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-falkenberg-elster-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-falkenberg-elster
    DOMAINNAME: bb-falkenberg-elster
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-herzberg-elster-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-herzberg-elster
    DOMAINNAME: herzberg-elster
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-michendorf-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-michendorf
    DOMAINNAME: bb-michendorf
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-amt-schlieben-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-amt-schlieben
    DOMAINNAME: bb-amt-schlieben
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-eisenhuettenstadt-build:
  extends: .docker_build_release
  variables:
    PROJECT: eisenhuettenstadt
    DOMAINNAME: eisenhuettenstadt
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-angermuende-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-angermuende
    DOMAINNAME: bb-angermuende
    NODENAME: portainer-14.docker.planetary-networks.de
    VOLUME_LOCALTIME: /etc/localtime:/etc/localtime:ro
bb-seelow-build:
  extends: .docker_build_release
  variables:
    PROJECT: bb-seelow
    DOMAINNAME: bb-seelow
    NODENAME: node-001.sva
    VOLUME_LOCALTIME: unused:/unused

#
# List of releases to deploy
#
bb-frankfurt-deploy:
  extends: .deploy_release
  needs: ["bb-frankfurt-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-frankfurt
    QUANTUM_ENDPOINT: portainer-14
bb-kyritz-deploy:
  extends: .deploy_release
  needs: ["bb-kyritz-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-kyritz
    QUANTUM_ENDPOINT: portainer-14
bb-birkenwerder-deploy:
  extends: .deploy_release
  needs: ["bb-birkenwerder-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-birkenwerder
    QUANTUM_ENDPOINT: portainer-14
bb-bad-belzig-deploy:
  extends: .deploy_release
  needs: ["bb-bad-belzig-build"]
  variables:
    SERVICENAME: ""
    PROJECT: bb-bad-belzig
    QUANTUM_ENDPOINT: portainer-14
bb-gransee-deploy:
  extends: .deploy_release
  needs: ["bb-gransee-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-gransee
    QUANTUM_ENDPOINT: portainer-14
bb-falkenberg-elster-deploy:
  extends: .deploy_release
  needs: ["bb-falkenberg-elster-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-falkenberg-elster
    QUANTUM_ENDPOINT: portainer-14
bb-herzberg-elster-deploy:
  extends: .deploy_release
  needs: ["bb-herzberg-elster-build"]
  variables:
    SERVICENAME: ""
    PROJECT: bb-herzberg-elster
    QUANTUM_ENDPOINT: portainer-14
bb-michendorf-deploy:
  extends: .deploy_release
  needs: ["bb-michendorf-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-michendorf
    QUANTUM_ENDPOINT: portainer-14
bb-amt-schlieben-deploy:
  extends: .deploy_release
  needs: ["bb-amt-schlieben-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-amt-schlieben
    QUANTUM_ENDPOINT: portainer-14
bb-eisenhuettenstadt-deploy:
  extends: .deploy_release
  needs: ["bb-eisenhuettenstadt-build"]
  variables:
    SERVICENAME: ""
    PROJECT: eisenhuettenstadt
    QUANTUM_ENDPOINT: portainer-14
bb-angermuende-deploy:
  extends: .deploy_release
  needs: ["bb-angermuende-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-angermuende
    QUANTUM_ENDPOINT: portainer-14
bb-seelow-deploy:
  extends: .deploy_release
  needs: ["bb-seelow-build"]
  variables:
    SERVICENAME: "-main_server"
    PROJECT: bb-seelow
    QUANTUM_ENDPOINT: sva
